// Screen for entering DVSA booking reference, theory pass, and license number
import 'package:flutter/material.dart';

class DvsaInfoScreen extends StatelessWidget {
  const DvsaInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DVSA Info'),
      ),
    );
  }
}
