// Screen for selecting DVSA test centres and date range
import 'package:flutter/material.dart';

class TestCentreSelectionScreen extends StatelessWidget {
  const TestCentreSelectionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Test Centre Selection'),
      ),
    );
  }
}
